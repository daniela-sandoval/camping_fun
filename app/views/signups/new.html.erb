<h1> Sign Up For An Activity</h1>

<%= form_for @signup do |f| %>
<%= f.label "Camper" %>
  <%= f.collection_select :camper_id, @campers, :id, :name %><br>
<%= f.label "Activity" %>
  <%= f.collection_select :activity_id, @activities, :id, :name %><br>
<%= f.label "Time" %>
  <%= f.select :time, (1..23) %><br>
  <br>
<%= f.submit %>
<% end %>
